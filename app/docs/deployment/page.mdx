# Deployment

Unforget runs anywhere PostgreSQL runs.

## Requirements

- PostgreSQL 15+ with pgvector extension
- Python 3.11+
- ~500MB RAM for embedding model (first load)

See [Docker setup](/docs/deployment/docker) and [performance tips](/docs/deployment/performance).
